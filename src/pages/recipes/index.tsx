type ComponentProps = {
  // onClick: MouseEventHandler;
  // children?: React.ReactNode;
};

export default function SearchResults({}: ComponentProps) {
  return (
    <>
      <h1>Search Results</h1>
    </>
  );
}
